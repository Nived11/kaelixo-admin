import DashboardMain from "@/features/dashboard/DashboardMain";

export default function DashboardPage() {
  // Keeping the route file 100% clean. All logic lives in the feature folder.
  return <DashboardMain />;
}